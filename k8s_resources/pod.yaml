apiVersion: v1
kind: Pod
metadata:
  name: <REPLACE-POD-NAME>
  labels: 
    app: <LABEL-NAME>
spec:
  containers:
    - name: app
      image: <IMAGE>
      ports:
      - containerPort: <CONTAINER-PORT>
  volumes: []